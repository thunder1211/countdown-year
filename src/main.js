import { createApp } from 'vue'
import './style.css'
import App from './App.vue'
// import VueCountdown from 'vue3-flip-countdown'

createApp(App).mount('#app')
// createApp(App).use(VueCountdown).mount('#app')
